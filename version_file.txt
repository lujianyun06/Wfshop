8.14 由于之前版本的依赖注解对arouter的版本用了“+”，这样虽然可以跟上最新版本，但一旦有了变化会导致整个工程跑不通了，这次就是因为跟上版本变化，而导致一个配置中的变量名moduleName变成了AROUTER_MODULE_NAME,原来的工程就不能用了，费了一天时间才找到问题所在。因此依赖最好不用“+”，而用确定的版本号。否则会出问题

8.14 由于后台给的category 页面的 json数据是一次吧所有的都给过来，所以数据处理上要做一些处理，并且加入netmanager，将url统一管理，并且category首次进去后由于没有点击操作，content页面没有更新，需添加一个数据加载完毕后对content页面的更新

8.15 17:28
	增加eventbus，以及相关的event，
	修正了搜索界面历史记录的显示顺序，点击事件
	search界面不发送网络请求，全部转移到搜索searchProduct中
	修正了search的url
	修正了一些bug
	
8.16 17:34
	把商品详情页面的购物车数字小红点的背景颜色用xml属性的方式赋予（之前是代码）
	把goods全部改成了product，保持和模块名一致

8.17 0:30
	把最后一页的其他设置单独作为一个fragment
	